service:
  name: reporting
  description: "Tools for generating and executing SQL queries against reporting databases."
system_instructions: |
  Reporting service tools:
  1. generate_reporting_sql_query → generate a SQL query from natural language input for reporting use cases using text-to-SQL service.
  2. execute_reporting_select_query → execute a SQL SELECT query against a tenant-specific reporting database with validation.
